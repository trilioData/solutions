#DELETE main FROM snapshot_vm_resource_metadata AS main INNER JOIN snapshot_vm_resources AS sub1 INNER JOIN snapshots AS sub2 WHERE main.snapshot_vm_resource_id = sub1.id AND sub1.snapshot_id = sub2.id AND sub2.deleted = 1
#SELECT * FROM snapshot_vm_resource_metadata AS del_table JOIN (SELECT main.id FROM snapshot_vm_resource_metadata AS main INNER JOIN snapshot_vm_resources AS sub1 INNER JOIN snapshots AS sub2 WHERE main.snapshot_vm_resource_id = sub1.id AND sub1.snapshot_id = sub2.id AND sub2.deleted = 1 LIMIT 1) del_select ON del_select.id = del_table.id;
DELETE del_table FROM snapshot_vm_resource_metadata AS del_table JOIN (SELECT main.id FROM snapshot_vm_resource_metadata AS main INNER JOIN snapshot_vm_resources AS sub1 INNER JOIN snapshots AS sub2 WHERE main.snapshot_vm_resource_id = sub1.id AND sub1.snapshot_id = sub2.id AND sub2.deleted = 1 LIMIT #LIMIT#) del_select ON del_select.id = del_table.id;
